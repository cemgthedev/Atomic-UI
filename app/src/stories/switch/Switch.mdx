import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';

import * as SwitchStories from "./Switch.stories";

<Meta of={SwitchStories} />

# Switch

Permite alternar rapidamente entre estados ligados e desligados.

## Padrão

Exibe o comportamento padrão de seleção binária.

<Canvas of={SwitchStories.Default} />

<Controls />

## Variantes

Veja as opções de estilo disponíveis.

<Canvas of={SwitchStories.Variants} />

## Tamanhos

Mostra como o switch aparece em cada tamanho.

<Canvas of={SwitchStories.Sizes} />
