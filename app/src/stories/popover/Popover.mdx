import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';

import * as PopoverStories from './Popover.stories';

<Meta of={PopoverStories} />

# Popover

Componente para exibir conteúdo contextual em uma camada flutuante.

## Padrão

Use o popover para mostrar informações adicionais sem sair da tela atual.

<Canvas of={PopoverStories.Default} />

<Controls />

## Align

Compare diferentes posicionamentos do conteúdo em relação ao trigger.

<Canvas of={PopoverStories.Align} />
