import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';

import * as DialogStories from './Dialog.stories';

<Meta of={DialogStories} />

# Dialog

Apresenta informações ou ações em uma janela modal sobreposta.

## Padrão

Use o Dialog para confirmar ações ou apresentar informações que precisam ser tratadas antes de continuar.

<Canvas of={DialogStories.Default} />

<Controls />

## Botão de fechar personalizado

Desabilite o botão de fechar padrão e apresente uma ação de fechamento no rodapé quando necessário.

<Canvas of={DialogStories.CustomCloseButton} />
