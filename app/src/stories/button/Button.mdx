import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';

import * as ButtonStories from "./Button.stories";

<Meta of={ButtonStories} />

# Botão

Permite iniciar ações, confirmações ou interações em diferentes contextos da interface.

## Padrão

Esta variante deve ser utilizada para a ação principal da tela.

<Canvas of={ButtonStories.Default} />

<Controls />

## Variantes

Exemplo de todas as variantes do botão.

<Canvas of={ButtonStories.Variants} />