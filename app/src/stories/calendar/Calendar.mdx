import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';

import * as CalendarStories from "./Calendar.stories";

<Meta of={CalendarStories} />

# Calendário

Calendário utilizado para selecionar datas.

## Padrão

Esta variante deve ser utilizada para a ação principal da tela.

<Canvas of={CalendarStories.Default} />

<Controls />

## Variantes

Exemplo de todas as variantes do calendário.

<Canvas of={CalendarStories.Variants} />